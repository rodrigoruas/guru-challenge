require 'rails_helper'

describe "user tests", :type => :request do
  # let!(:users) { FactoryBot.create(:user) }
  # it "check if subscription is updated" do
  #   headers = { "CONTENT_TYPE" => "application/json" }
  #   post "/subscriptions", :params => '{ "subscription": "Premium" }', :headers => headers
  #   expect(response["subscription"].to eq("Premium"))
  # end
end